local options = {
  actions = {
    open_file = {
      quit_on_open = true,
      window_picker = {
        enable = false,
      },
    },
  },

  view = {
    width = 60,
    side = "right",
  },
}

return options
